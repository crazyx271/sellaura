{
  "name": "sellaura",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "fonts": "node scripts/copy-fonts.js",
    "build:css": "tailwindcss -i src/input.css -o public/css/app.css --minify",
    "build": "npm run fonts && npm run build:css",
    "test": "node --test test/*.test.js"
  },
  "devDependencies": {
    "@fontsource/manrope": "^5.2.6",
    "tailwindcss": "^3.4.17"
  }
}
